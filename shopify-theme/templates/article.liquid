<section class="pagehead section">
  <div class="container">
    <span class="mlabel">{{ article.published_at | date: '%B %d, %Y' }}</span>
    <h1 class="d1" style="margin-top: 16px">{{ article.title }}</h1>
    <div class="measure rte" style="margin-top: 32px">{{ article.content }}</div>
  </div>
</section>
